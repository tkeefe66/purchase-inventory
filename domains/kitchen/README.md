# Kitchen domain — not implemented

Per the **Golden Rule** in `CLAUDE.md` and `docs/PLAN.md`: ship Outdoor end-to-end before starting any second domain.

Kitchen is a **deferred** domain (Phase 7+). Until Outdoor is in daily use through Phase 6 (or Phase 5 if web UI is deferred), nothing in this folder gets built.

Items that look kitchen-ish today are routed to `Domain=Other` in the sheet. They can be reclassified in bulk when this domain ships.

See `docs/PLAN.md` Phase 7 for the extension procedure.
